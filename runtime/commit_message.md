ヒットボックスデバッグ表示の対象漏れと可視化漏れを修正

・ボスのデバッグ対象グループ名を統一してヒットボックス描画対象から漏れないようにした
・無効中の当たり判定形状も半透明で描画して実判定とデバッグ可視化を分離した
・自機の移動入力参照を安全化して起動直後の入力未登録時でもエラーを出さないようにした
・ヘッドレスでゲーム画面を短時間起動してエラーが出ないことを確認した
・画面あり起動を短時間実行して起動エラーがないことを確認した
・ボスと自機のデバッグ表示条件を検証して通常時の自機当たり判定無効が維持されることを確認した